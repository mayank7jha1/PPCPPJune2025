#include<iostream>
#include<vector>
using namespace std;

vector<double> Solve(vector<int>ans) {

	vector<double>p3;


	return p3;
	return {1.5, 2.9, 3, 4, 5};

	// return 5;
}

int Solve2(vector<int>ans) {



}

vector<int>Solve3(int x) {

}



int main() {

	//Define a vector
	vector<int>v;


	vector<int>v1{2, 3, 1, 4, 5};
	vector<int>v2(5);//This is a vector of size 5 with value 0.
	vector<int>v3(9, 3);//THis is a vector of size 9 with each bucket containing value 3.

	vector<int>v4 = v3;

	//Kitne currently elements are present in vector
	cout << v1.size() << endl;
	cout << v1[2] << endl;

	//Every Container can be treated as a Data type.
	Solve(v1);


	Solve2(v1);
	Solve3(10);



}